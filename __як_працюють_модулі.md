# 📚 Довідник: Як працюють модулі в проекті

## Що таке модулі?

**Модуль** - це окремий Python файл, який містить функції та змінні. Модулі допомагають:
- 📦 Організувати код
- 🔄 Використовувати функції в різних файлах
- 🎯 Розділити відповідальність
## Структура нашого проекту

```
retail_project/
│
├── database.py           # Модуль для роботи з JSON
├── retail_strogage.py    # Модуль з бізнес-логікою
└── main.py              # Головна програма
```
## 1. Модуль `database.py` - Робота з файлами

### Що містить:
```python
import json
from pathlib import Path

# Глобальна змінна - шлях до бази даних
DB_PATH = Path("data/accounts.json")

def load_database():
    """Завантажує дані з JSON"""
    # код функції...

def save_database(database):
    """Зберігає дані у JSON"""
    # код функції...
```

### Як це працює:

**Глобальна змінна `DB_PATH`:**
- Створюється один раз при імпорті модуля
- Доступна всім функціям у модулі
- Зберігає шлях до файлу бази даних

**Функції:**
- `load_database()` - читає JSON файл
- `save_database()` - записує у JSON файл
- `backup_database()` - створює резервну копію

### Приклад використання в інших файлах:

```python
import database as db

# Завантажити базу
data = db.load_database()

# Зберегти зміни
db.save_database(data)
```
## 2. Модуль `retail_strogage.py` - Бізнес-логіка

### Що містить:
```python
import database as db

def calculate_discount(price, category):
    """Розраховує знижку"""
    # код функції...

def create_account(bayer_id, database, initial_balance=0):
    """Створює рахунок"""
    # код функції...
```

### Як це працює:

**Імпорт іншого модуля:**
```python
import database as db
```
- Імпортуємо модуль `database`
- Даємо йому коротку назву `db`
- Тепер можемо викликати: `db.load_database()`

**Використання функцій з database.py:**
```python
def get_balance(bayer_id):
    database = db.load_database()  # ← викликаємо функцію з database.py
    return database[bayer_id]["balance"]
```
## 3. Модуль `main.py` - Інтерфейс

### Що містить:
```python
import retail_strogage as rs
import database as db

def create_account_menu():
    """Меню створення рахунку"""
    database = db.load_database()
    rs.create_account(buyer_id, database, balance)
```

### Як це працює:

**Імпорт двох модулів:**
```python
import retail_strogage as rs  # Функції для рахунків
import database as db          # Функції для JSON
```

**Виклик функцій з різних модулів:**
```python
database = db.load_database()        # ← з database.py
rs.create_account(id, database, 0)  # ← з retail_strogage.py
```
## Як працює імпорт?

### Приклад 1: Простий імпорт
```python
import database

# Виклик функції
database.load_database()
```

### Приклад 2: Імпорт з коротким ім'ям
```python
import database as db

# Виклик функції (коротше!)
db.load_database()
```

### Приклад 3: Імпорт конкретних функцій
```python
from database import load_database, save_database

# Виклик без префіксу
load_database()
```

**У нашому проекті ми використовуємо Приклад 2** - це зручно та зрозуміло!
## Порядок виконання при запуску

### Коли ви запускаєте `python main.py`:

1. **Python читає main.py**
   ```python
   import retail_strogage as rs
   import database as db
   ```

2. **Python виконує database.py**
   - Створює змінну `DB_PATH`
   - Визначає функції
   - Викликає `ensure_database_exists()` (створює папку та файл)

3. **Python виконує retail_strogage.py**
   - Імпортує `database as db`
   - Визначає всі функції

4. **Python продовжує main.py**
   - Тепер всі функції доступні
   - Запускається `main()`
## Чому ми не використовуємо класи?

### Замість класу:
```python
# Складно для початківців
class DatabaseManager:
    def __init__(self):
        self.path = "..."
    
    def load(self):
        # ...

db = DatabaseManager()
db.load()
```

### Ми використовуємо функції:
```python
# Просто та зрозуміло
DB_PATH = "..."

def load_database():
    # ...

load_database()
```

**Переваги для навчання:**
- ✅ Простіше зрозуміти
- ✅ Менше нового синтаксису
- ✅ Фокус на логіці, а не на ООП
- ✅ Класи вивчите пізніше!
## Глобальні змінні в модулях

### Що таке глобальна змінна в модулі?

```python
# database.py
DB_PATH = Path("data/accounts.json")  # ← Глобальна змінна модуля
```

**Особливості:**
- Створюється один раз при імпорті
- Доступна всім функціям у модулі
- НЕ можна змінити з інших модулів (захист від помилок)

### Приклад використання:

```python
# database.py
DB_PATH = Path("data/accounts.json")

def load_database():
    with open(DB_PATH, 'r') as file:  # ← Використовуємо DB_PATH
        return json.load(file)

def save_database(data):
    with open(DB_PATH, 'w') as file:  # ← Використовуємо DB_PATH
        json.dump(data, file)
```

**Чому це зручно?**
- Шлях до файлу написаний один раз
- Легко змінити (тільки в одному місці)
- Всі функції використовують той самий шлях
## Поширені помилки та їх вирішення

### Помилка 1: ModuleNotFoundError
```
ModuleNotFoundError: No module named 'database'
```

**Причина:** Python не знаходить файл `database.py`

**Рішення:**
- Переконайтеся, що файл `database.py` у тій самій папці
- Запускайте програму з правильної папки
### Помилка 2: AttributeError
```
AttributeError: module 'database' has no attribute 'load'
```

**Причина:** Неправильна назва функції

**Рішення:**
- Перевірте назву: `load_database()`, а не `load()`
- Функція повинна бути визначена в модулі
### Помилка 3: Циклічний імпорт
```
ImportError: cannot import name '...' from partially initialized module
```

**Причина:** Модулі імпортують один одного

**У нашому проекті:**
```
main.py → імпортує retail_strogage.py
retail_strogage.py → імпортує database.py
database.py → нікого не імпортує ✓
```

**Правило:** Створюйте ланцюжок без циклів!
## Завдання для перевірки розуміння

### Завдання 1: Базове
Що виведе цей код?

```python
# file1.py
MESSAGE = "Привіт"

def greet():
    print(MESSAGE)

# file2.py
import file1

file1.greet()  # Що виведе?
```

<details>
<summary>Відповідь</summary>
Виведе: "Привіт"
</details>
### Завдання 2: Середнє
Доповніть код:

```python
# calculator.py
def add(a, b):
    return a + b

# main.py
import calculator as calc

result = calc.???(5, 3)  # Що тут написати?
print(result)  # Має вивести 8
```

<details>
<summary>Відповідь</summary>
calc.add(5, 3)
</details>
### Завдання 3: Складне
Створіть свій власний модуль `helpers.py` з функцією `double(x)`, яка повертає x * 2. Використайте її в `main.py`.

<details>
<summary>Рішення</summary>

```python
# helpers.py
def double(x):
    return x * 2

# main.py
import helpers

result = helpers.double(5)
print(result)  # 10
```
</details>
## Підсумок

### Що ви навчилися:

✅ **Модулі** - окремі файли з функціями  
✅ **import** - імпорт модулів у свій код  
✅ **as** - короткі назви для модулів  
✅ **Глобальні змінні** - змінні на рівні модуля  
✅ **Організація коду** - розділення на логічні частини  

### Ключові правила:

1. **Один модуль = одна відповідальність**
   - `database.py` - тільки робота з JSON
   - `retail_strogage.py` - тільки бізнес-логіка
   - `main.py` - тільки інтерфейс

2. **Імпортуйте тільки те, що потрібно**
   ```python
   import database as db  # Весь модуль
   # АБО
   from database import load_database  # Тільки одну функцію
   ```

3. **Не бійтеся експериментувати!**
   - Створюйте власні модулі
   - Пробуйте різні способи імпорту
   - Дивіться, що працює краще
## Додаткові матеріали

### Як подивитися, що є в модулі?

```python
import database

print(dir(database))  # Список всіх функцій та змінних
```

### Як подивитися документацію функції?

```python
import database

print(database.load_database.__doc__)  # Виведе docstring
help(database.load_database)           # Детальна довідка
```
**Тепер ви готові працювати з модулями! 🎉**