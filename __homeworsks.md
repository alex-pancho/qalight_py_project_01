# 📝 20 завдань для самостійної доробки проекту

## Рівень 1: Прості завдання (1-7)
*Час виконання: 10-20 хвилин кожне*
### Завдання 1: Додати функцію підрахунку загальної суми на всіх рахунках

**Файл:** `retail.py`

**Що зробити:**
```python
def get_total_balance():
    """
    Повертає загальну суму на всіх рахунках.
    
    Returns:
        float: Сума всіх балансів
    """
    # Ваш код тут
    pass
```

**Підказка:**
1. Завантажте базу даних: `database = db.load_database()`
2. Пройдіться циклом по всіх рахунках
3. Додайте кожен `balance` до загальної суми

**Перевірка:**
- Створіть 3 рахунки з балансами 100, 200, 300
- Викличте функцію - має повернути 600
### Завдання 2: Додати пункт меню "Загальний баланс системи"

**Файл:** `main.py`

**Що зробити:**
1. Додайте в `print_menu()` новий пункт "11. Загальний баланс"
2. Створіть функцію:
```python
def total_balance_menu():
    """Показує загальний баланс всіх рахунків."""
    total = rs.get_total_balance()
    print(f"\n💰 Загальний баланс системи: {total:.2f} грн")
```
3. Додайте виклик у `main()` при виборі "11"

**Перевірка:**
- Запустіть програму
- Оберіть пункт 11
- Має показати суму всіх рахунків
### Завдання 3: Додати перевірку на максимальну суму зняття

**Файл:** `retail.py`

**Що зробити:**
Додайте в функцію `withdraw()` після рядка `if amount <= 0:`:
```python
MAX_WITHDRAW = 10000  # Максимум 10000 грн за раз

if amount > MAX_WITHDRAW:
    raise ValueError(f"Максимальна сума зняття: {MAX_WITHDRAW} грн")
```

**Перевірка:**
- Спробуйте зняти 15000 грн
- Має вивести помилку
### Завдання 4: Додати дату до транзакцій

**Файл:** `retail.py`

**Що зробити:**
1. Додайте імпорт на початку файлу:
```python
from datetime import datetime
```

2. У функціях `deposit()` та `withdraw()` змініть створення `transaction`:
```python
transaction = {
    "type": "deposit",
    "amount": amount,
    "balance_after": database[bayer_id]["balance"],
    "date": datetime.now().strftime("%Y-%m-%d %H:%M:%S")  # ← ДОДАЙТЕ ЦЕЙ РЯДОК
}
```

**Перевірка:**
- Поповніть рахунок
- Подивіться інформацію про рахунок
- Має показати дату транзакції
### Завдання 5: Показувати категорію при створенні рахунку

**Файл:** `main.py`

**Що зробити:**
У функції `create_account_menu()` після рядка `initial_balance = ...` додайте:
```python
print("\nДоступні категорії:")
print("  1. Regular (без знижок)")
print("  2. Student (знижка 10%)")
print("  3. VIP (знижка 20%)")
category_choice = input("Оберіть категорію (1-3, Enter для Regular): ").strip()

# Визначаємо категорію
if category_choice == "2":
    category = "Student"
elif category_choice == "3":
    category = "VIP"
else:
    category = "Regular"
```

Потім у функції `create_account()` в `retail.py` змініть:
```python
def create_account(bayer_id: str, database: dict, initial_balance: float = 0, category: str = "Regular"):
```

І в `account`:
```python
account = {
    "bayer_id": bayer_id,
    "balance": initial_balance,
    "category": category,  # ← Використовуємо параметр
    "transaction_history": []
}
```

**Перевірка:**
- Створіть рахунок і оберіть категорію Student
- Подивіться інформацію - має показати категорію
### Завдання 6: Додати функцію пошуку рахунку за категорією

**Файл:** `retail.py`

**Що зробити:**
```python
def find_accounts_by_category(category):
    """
    Знаходить всі рахунки певної категорії.
    
    Args:
        category (str): Категорія для пошуку
    
    Returns:
        dict: Словник з рахунками цієї категорії
    """
    database = db.load_database()
    result = {}
    
    for bayer_id, account in database.items():
        if account.get("category", "Regular").upper() == category.upper():
            result[bayer_id] = account
    
    return result
```

**Перевірка:**
- Створіть 2 VIP рахунки і 1 Student
- Викличте `find_accounts_by_category("VIP")`
- Має повернути 2 рахунки
### Завдання 7: Додати меню пошуку за категорією

**Файл:** `main.py`

**Що зробити:**
```python
def search_by_category_menu():
    """Меню пошуку рахунків за категорією."""
    print("\n🔍 Пошук за категорією")
    print("-" * 40)
    print("Категорії: Regular, Student, VIP")
    
    category = input("Введіть категорію: ").strip()
    accounts = rs.find_accounts_by_category(category)
    
    if not accounts:
        print(f"\n⚠️  Рахунків категорії '{category}' не знайдено")
        return
    
    print(f"\nЗнайдено рахунків: {len(accounts)}\n")
    for bayer_id, account in accounts.items():
        print(f"├─ {bayer_id}: {account['balance']:.2f} грн")
```

Додайте пункт "12. Пошук за категорією" в меню.
## Рівень 2: Середні завдання (8-14)
*Час виконання: 20-30 хвилин кожне*
### Завдання 8: Функція зміни категорії клієнта

**Файл:** `retail.py`

**Що зробити:**
```python
def change_category(bayer_id, new_category):
    """
    Змінює категорію клієнта.
    
    Args:
        bayer_id (str): ID покупця
        new_category (str): Нова категорія (Regular, Student, VIP)
    
    Returns:
        str: Нова категорія
    """
    valid_categories = ["REGULAR", "STUDENT", "VIP"]
    
    if new_category.upper() not in valid_categories:
        raise ValueError("Невірна категорія. Доступні: Regular, Student, VIP")
    
    database = db.load_database()
    
    if bayer_id not in database:
        raise ValueError(f"Рахунок '{bayer_id}' не знайдено")
    
    # Ваш код: змініть категорію і збережіть базу
    pass
```

**Підказка:** 
- `database[bayer_id]["category"] = new_category`
- `db.save_database(database)`
### Завдання 9: Функція переказу між рахунками

**Файл:** `retail.py`

**Що зробити:**
```python
def transfer(from_id, to_id, amount):
    """
    Переказує кошти з одного рахунку на інший.
    
    Args:
        from_id (str): ID відправника
        to_id (str): ID отримувача
        amount (float): Сума переказу
    
    Returns:
        bool: True якщо успішно
    """
    # Перевірки:
    # 1. amount > 0
    # 2. Обидва рахунки існують
    # 3. Достатньо коштів у from_id
    
    # Дії:
    # 1. Зняти з from_id (withdraw)
    # 2. Додати до to_id (deposit)
    # 3. Додати запис про переказ в історію обох рахунків
    
    pass
```

**Перевірка:**
- Створіть 2 рахунки
- Переведіть 100 грн з одного на інший
- Перевірте баланси обох
### Завдання 10: Функція показу історії транзакцій

**Файл:** `retail.py`

**Що зробити:**
```python
def get_transaction_history(bayer_id, limit=10):
    """
    Повертає останні N транзакцій.
    
    Args:
        bayer_id (str): ID покупця
        limit (int): Кількість останніх транзакцій
    
    Returns:
        list: Список транзакцій
    """
    database = db.load_database()
    
    if bayer_id not in database:
        raise ValueError(f"Рахунок '{bayer_id}' не знайдено")
    
    history = database[bayer_id]["transaction_history"]
    
    # Поверніть останні limit транзакцій
    # Підказка: використайте зріз списку history[-limit:]
    pass
```
### Завдання 11: Меню перегляду історії транзакцій

**Файл:** `main.py`

**Що зробити:**
```python
def transaction_history_menu():
    """Меню перегляду історії транзакцій."""
    print("\n📜 Історія транзакцій")
    print("-" * 40)
    
    try:
        bayer_id = input("ID покупця: ").strip()
        limit_input = input("Скільки показати (Enter для всіх): ").strip()
        limit = int(limit_input) if limit_input else 999
        
        history = rs.get_transaction_history(bayer_id, limit)
        
        if not history:
            print("\n⚠️  Немає транзакцій")
            return
        
        print(f"\nОстанні {len(history)} транзакцій:\n")
        for i, trans in enumerate(history, 1):
            print(f"{i}. Тип: {trans['type']}")
            print(f"   Сума: {trans['amount']:.2f} грн")
            print(f"   Баланс після: {trans['balance_after']:.2f} грн")
            if 'date' in trans:
                print(f"   Дата: {trans['date']}")
            print()
            
    except ValueError as e:
        print(f"\n❌ Помилка: {e}")
```
### Завдання 12: Експорт списку рахунків у текстовий файл

**Файл:** `retail.py`

**Що зробити:**
```python
def export_accounts_to_txt(filename="accounts_report.txt"):
    """
    Експортує список всіх рахунків у текстовий файл.
    
    Args:
        filename (str): Назва файлу для експорту
    """
    database = db.load_database()
    
    with open(filename, 'w', encoding='utf-8') as file:
        file.write("=" * 50 + "\n")
        file.write("ЗВІТ ПРО РАХУНКИ\n")
        file.write("=" * 50 + "\n\n")
        
        for bayer_id, account in database.items():
            file.write(f"ID: {bayer_id}\n")
            file.write(f"Баланс: {account['balance']:.2f} грн\n")
            file.write(f"Категорія: {account['category']}\n")
            file.write(f"Транзакцій: {len(account['transaction_history'])}\n")
            file.write("-" * 50 + "\n")
    
    print(f"✓ Звіт збережено у файл: {filename}")
```

**Перевірка:**
- Викличте функцію
- Відкрийте файл `accounts_report.txt`
- Має бути красиво відформатований звіт
### Завдання 13: Функція статистики системи

**Файл:** `retail.py`

**Що зробити:**
```python
def get_system_statistics():
    """
    Повертає статистику системи.
    
    Returns:
        dict: Словник зі статистикою
    """
    database = db.load_database()
    
    stats = {
        "total_accounts": 0,
        "total_balance": 0,
        "vip_count": 0,
        "student_count": 0,
        "regular_count": 0,
        "total_transactions": 0
    }
    
    # Ваш код:
    # Пройдіться по всіх рахунках
    # Порахуйте кількість рахунків кожної категорії
    # Додайте всі баланси
    # Порахуйте загальну кількість транзакцій
    
    return stats
```

**Підказка:**
```python
for bayer_id, account in database.items():
    stats["total_accounts"] += 1
    stats["total_balance"] += account["balance"]
    # і т.д.
```
### Завдання 14: Меню статистики

**Файл:** `main.py`

**Що зробити:**
```python
def statistics_menu():
    """Меню статистики системи."""
    print("\n📊 Статистика системи")
    print("-" * 40)
    
    stats = rs.get_system_statistics()
    
    print(f"\nЗагальна інформація:")
    print(f"  Всього рахунків: {stats['total_accounts']}")
    print(f"  Загальний баланс: {stats['total_balance']:.2f} грн")
    print(f"  Всього транзакцій: {stats['total_transactions']}")
    
    print(f"\nРозподіл по категоріях:")
    print(f"  VIP клієнтів: {stats['vip_count']}")
    print(f"  Студентів: {stats['student_count']}")
    print(f"  Звичайних: {stats['regular_count']}")
```
## Рівень 3: Складні завдання (15-20)
*Час виконання: 30-45 хвилин кожне*
### Завдання 15: Функція розрахунку покупки зі знижкою

**Файл:** `retail.py`

**Що зробити:**
```python
def calculate_purchase(bayer_id, price, quantity):
    """
    Розраховує вартість покупки з урахуванням знижок клієнта та кількості.
    
    Args:
        bayer_id (str): ID покупця
        price (float): Ціна одного товару
        quantity (int): Кількість
    
    Returns:
        dict: Інформація про покупку
    """
    database = db.load_database()
    
    if bayer_id not in database:
        raise ValueError(f"Рахунок '{bayer_id}' не знайдено")
    
    category = database[bayer_id].get("category", "Regular")
    
    # 1. Розрахуйте базову вартість: price * quantity
    # 2. Застосуйте оптову знижку: calculate_bulk_discount()
    # 3. Застосуйте знижку категорії: calculate_discount()
    # 4. Поверніть словник з детальною інформацією
    
    result = {
        "base_price": 0,
        "after_bulk_discount": 0,
        "final_price": 0,
        "total_discount": 0,
        "category": category
    }
    
    # Ваш код тут
    
    return result
```

**Перевірка:**
- VIP клієнт купує 100 товарів по 50 грн
- Має бути: оптова знижка 15% + VIP знижка 20%
### Завдання 16: Меню розрахунку покупки

**Файл:** `main.py`

**Що зробити:**
```python
def purchase_calculator_menu():
    """Калькулятор покупки з урахуванням всіх знижок."""
    print("\n🛒 Калькулятор покупки")
    print("-" * 40)
    
    try:
        bayer_id = input("ID покупця: ").strip()
        price = float(input("Ціна товару (грн): "))
        quantity = int(input("Кількість: "))
        
        result = rs.calculate_purchase(bayer_id, price, quantity)
        
        print(f"\n{'='*40}")
        print(f"Категорія клієнта: {result['category']}")
        print(f"Базова вартість: {result['base_price']:.2f} грн")
        print(f"Після оптової знижки: {result['after_bulk_discount']:.2f} грн")
        print(f"Фінальна ціна: {result['final_price']:.2f} грн")
        print(f"Загальна економія: {result['total_discount']:.2f} грн")
        print(f"{'='*40}")
        
        # Запитати чи списати з рахунку
        confirm = input("\nСписати з рахунку? (так/ні): ").strip().lower()
        if confirm in ['так', 'yes', 'y', 'т']:
            rs.withdraw(bayer_id, result['final_price'])
            
    except ValueError as e:
        print(f"\n❌ Помилка: {e}")
```
### Завдання 17: Функція пошуку рахунків з балансом нижче мінімуму

**Файл:** `retail.py`

**Що зробити:**
```python
def find_low_balance_accounts(min_balance=100):
    """
    Знаходить рахунки з балансом нижче мінімального.
    
    Args:
        min_balance (float): Мінімальний баланс
    
    Returns:
        list: Список кортежів (bayer_id, balance)
    """
    database = db.load_database()
    low_accounts = []
    
    for bayer_id, account in database.items():
        if account["balance"] < min_balance:
            low_accounts.append((bayer_id, account["balance"]))
    
    # Відсортуйте за балансом (від найменшого)
    low_accounts.sort(key=lambda x: x[1])
    
    return low_accounts
```

**Перевірка:**
- Створіть рахунки з балансами: 50, 150, 30, 200
- Викличте з min_balance=100
- Має повернути: [('id3', 30), ('id1', 50)]
### Завдання 18: Функція автоматичного нагадування про низький баланс

**Файл:** `main.py`

**Що зробити:**
Додайте функцію, яка викликається при кожному запуску програми:

```python
def check_low_balances():
    """Перевіряє та показує попередження про низький баланс."""
    low_accounts = rs.find_low_balance_accounts(min_balance=100)
    
    if low_accounts:
        print("\n⚠️  УВАГА! Рахунки з низьким балансом:")
        for bayer_id, balance in low_accounts[:5]:  # Показуємо максимум 5
            print(f"   • {bayer_id}: {balance:.2f} грн")
        print()
```

Викличте цю функцію в `main()` на початку циклу.
### Завдання 19: Експорт транзакцій у CSV

**Файл:** `retail.py`

**Що зробити:**
```python
def export_transactions_to_csv(bayer_id, filename="transactions.csv"):
    """
    Експортує історію транзакцій у CSV файл.
    
    Args:
        bayer_id (str): ID покупця
        filename (str): Назва CSV файлу
    """
    import csv
    
    database = db.load_database()
    
    if bayer_id not in database:
        raise ValueError(f"Рахунок '{bayer_id}' не знайдено")
    
    transactions = database[bayer_id]["transaction_history"]
    
    with open(filename, 'w', newline='', encoding='utf-8') as file:
        writer = csv.writer(file)
        
        # Заголовки
        writer.writerow(['Тип', 'Сума', 'Баланс після', 'Дата'])
        
        # Дані
        for trans in transactions:
            writer.writerow([
                trans['type'],
                trans['amount'],
                trans['balance_after'],
                trans.get('date', 'Не вказано')
            ])
    
    print(f"✓ Експорт завершено: {filename}")
```

**Перевірка:**
- Поповніть рахунок кілька разів
- Експортуйте транзакції
- Відкрийте CSV у Excel або блокноті
### Завдання 20: Система бонусів (складне!)

**Файл:** `retail.py`

**Що зробити:**

1. Додайте поле `bonus_points` при створенні рахунку:
```python
account = {
    "bayer_id": bayer_id,
    "balance": initial_balance,
    "category": category,
    "transaction_history": [],
    "bonus_points": 0  # ← ДОДАЙТЕ ЦЕ
}
```

2. Створіть функцію нарахування бонусів:
```python
def add_bonus_points(bayer_id, amount):
    """
    Нараховує бонусні бали за покупку.
    Правило: 1 бал за кожні 10 грн витрачених коштів.
    
    Args:
        bayer_id (str): ID покупця
        amount (float): Сума покупки
    
    Returns:
        int: Кількість нарахованих балів
    """
    database = db.load_database()
    
    if bayer_id not in database:
        raise ValueError(f"Рахунок '{bayer_id}' не знайдено")
    
    points = int(amount // 10)  # 1 бал за кожні 10 грн
    
    if "bonus_points" not in database[bayer_id]:
        database[bayer_id]["bonus_points"] = 0
    
    database[bayer_id]["bonus_points"] += points
    db.save_database(database)
    
    print(f"✓ Нараховано {points} бонусних балів")
    return points
```

3. Змініть функцію `withdraw()` - додайте виклик `add_bonus_points()` після успішного зняття:
```python
# В кінці функції withdraw() додайте:
add_bonus_points(bayer_id, amount)
```

4. Створіть функцію використання бонусів:
```python
def use_bonus_points(bayer_id, points):
    """
    Обмінює бонусні бали на гривні.
    Правило: 100 балів = 10 грн
    
    Args:
        bayer_id (str): ID покупця
        points (int): Кількість балів для обміну
    """
    database = db.load_database()
    
    if bayer_id not in database:
        raise ValueError(f"Рахунок '{bayer_id}' не знайдено")
    
    current_points = database[bayer_id].get("bonus_points", 0)
    
    if points > current_points:
        raise ValueError(f"Недостатньо балів. Доступно: {current_points}")
    
    if points < 100:
        raise ValueError("Мінімум для обміну: 100 балів")
    
    # Розрахунок
    money = (points // 100) * 10  # 100 балів = 10 грн
    used_points = (points // 100) * 100  # Використовуємо цілі сотні
    
    # Оновлення
    database[bayer_id]["bonus_points"] -= used_points
    database[bayer_id]["balance"] += money
    
    db.save_database(database)
    
    print(f"✓ Обмінено {used_points} балів на {money:.2f} грн")
    print(f"  Залишилось балів: {database[bayer_id]['bonus_points']}")
```

**Перевірка:**
- Створіть рахунок з балансом 1000 грн
- Зніміть 500 грн → має нарахуватися 50 балів
- Зніміть ще 500 грн → буде 100 балів
- Обміняйте 100 балів → отримаєте 10 грн на баланс
## 📋 Чек-лист виконання завдань

Відмічайте виконані завдання:

**Рівень 1 (прості):**
- [ ] 1. Функція загального балансу
- [ ] 2. Меню загального балансу
- [ ] 3. Ліміт на зняття
- [ ] 4. Дата в транзакціях
- [ ] 5. Вибір категорії при створенні
- [ ] 6. Пошук за категорією (функція)
- [ ] 7. Пошук за категорією (меню)

**Рівень 2 (середні):**
- [ ] 8. Зміна категорії
- [ ] 9. Переказ між рахунками
- [ ] 10. Історія транзакцій (функція)
- [ ] 11. Історія транзакцій (меню)
- [ ] 12. Експорт у TXT
- [ ] 13. Статистика (функція)
- [ ] 14. Статистика (меню)

**Рівень 3 (складні):**
- [ ] 15. Розрахунок покупки (функція)
- [ ] 16. Розрахунок покупки (меню)
- [ ] 17. Пошук низького балансу
- [ ] 18. Автоматичне нагадування
- [ ] 19. Експорт у CSV
- [ ] 20. Система бонусів
## 🎯 Рекомендації по виконанню

### Як працювати з завданнями:

1. **Починайте з рівня 1** - це прості завдання для розігріву
2. **Тестуйте кожне завдання** перед переходом до наступного
3. **Не бійтеся помилок** - це нормальна частина навчання
4. **Використовуйте print()** для налагодження
5. **Читайте підказки** - вони допоможуть у складних місцях

### Якщо застрягли:

1. Перечитайте завдання уважно
2. Подивіться на схожі функції в коді
3. Використайте `print()` для виведення проміжних результатів
4. Запитайте у викладача або однокурсників
5. Зробіть перерву і поверніться пізніше

### Після виконання:

- ✅ Перевірте, чи працює програма без помилок
- ✅ Протестуйте різні варіанти вводу
- ✅ Додайте коментарі до свого коду
- ✅ Покажіть викладачу для перевірки
## 🏆 Бонусні ідеї (для найсильніших)

Якщо ви виконали всі 20 завдань, спробуйте:

- **Система знижкових купонів** - можна застосувати купон на покупку
- **Реферальна програма** - бонуси за залучення нових клієнтів
- **Ліміти операцій** - максимум 5