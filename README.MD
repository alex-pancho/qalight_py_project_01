# 🏪 Система обліку роздрібної мережі

Консольний додаток для управління рахунками покупців та розрахунку знижок у роздрібній мережі.
## 📋 Що реалізовано

### 1. Структура проекту
```
retail_project/
│
├── retail_storage.py      # Бізнес-логіка (функції роботи з рахунками)
├── database.py            # Робота з JSON базою даних
├── main.py               # Консольний інтерфейс користувача
├── data/
│   └── accounts.json     # JSON файл з даними рахунків
└── README.md             # Документація
```
## 🎯 Функціональність

### Робота з рахунками:
- ✅ Створення нового рахунку покупця
- ✅ Перегляд балансу
- ✅ Поповнення рахунку
- ✅ Зняття коштів
- ✅ Перегляд детальної інформації про рахунок
- ✅ Список всіх рахунків
- ✅ Закриття рахунку
- ✅ Історія транзакцій

### Калькулятори знижок:
- ✅ Знижки за категорією клієнта (VIP, Student)
- ✅ Оптові знижки за кількість товару

### Робота з даними:
- ✅ Збереження даних у JSON
- ✅ Автоматичне створення бази даних
- ✅ Резервне копіювання
## 🚀 Як запустити

### 1. Переконайтеся, що встановлено Python 3.7+
```bash
python --version
```

### 2. Запустіть програму
```bash
python main.py
```

### 3. Оберіть опцію з меню
## 📚 Детальний опис модулів

### `database.py` - Модуль роботи з JSON

**Мета:** Відокремити логіку збереження даних від бізнес-логіки.

**Функції:**
- `ensure_database_exists()` - створює базу даних, якщо її немає
- `load_database()` - завантаження даних з JSON
- `save_database(data)` - збереження даних у JSON
- `backup_database()` - створення резервної копії
- `count_accounts()` - підрахунок кількості рахунків

**Навіщо:**
- Полегшує зміну способу збереження даних
- Централізує роботу з файлами
- Автоматично створює базу даних при першому запуску

**Приклад використання:**
```python
import database as db

# Завантажити базу даних
data = db.load_database()

# Зберегти зміни
db.save_database(data)

# Створити резервну копію
db.backup_database()
```
### `retail.py` - Бізнес-логіка

**Функції для розрахунку знижок:**

#### `calculate_discount(price, category)`
Розраховує знижку за категорією клієнта.

**Знижки:**
- VIP: 20%
- Student: 10%
- Regular: 0%

**Приклад:**
```python
price = 1000
final_price = calculate_discount(price, "VIP")  # 800 грн
```

#### `calculate_bulk_discount(price, quantity)`
Розраховує оптову знижку за кількість.

**Знижки:**
- 10-49 шт: 5%
- 50-99 шт: 10%
- 100+ шт: 15%

**Приклад:**
```python
price = 100
quantity = 50
total = calculate_bulk_discount(price, quantity)  # 4500 грн (знижка 10%)
```
**Функції для роботи з рахунками:**

#### `create_account(buyer_id, initial_balance, category)`
Створює новий рахунок покупця.

**Параметри:**
- `buyer_id` - унікальний ідентифікатор
- `initial_balance` - початковий баланс (за замовчуванням 0)
- `category` - категорія клієнта (Regular, Student, VIP)

**Структура рахунку:**
```python
{
    "buyer_id": "user123",
    "balance": 0,
    "category": "Regular",
    "transaction_history": []
}
```

#### `get_balance(buyer_id)`
Повертає поточний баланс рахунку.

#### `deposit(buyer_id, amount)`
Поповнює рахунок на вказану суму.

**Що відбувається:**
1. Перевіряється існування рахунку
2. Додається сума до балансу
3. Записується транзакція в історію
4. Зберігаються зміни в JSON

#### `withdraw(buyer_id, amount)`
Знімає кошти з рахунку.

**Перевірки:**
- Чи існує рахунок
- Чи достатньо коштів на рахунку
- Чи додатня сума зняття

#### `close_account(buyer_id)`
Закриває рахунок (видаляє з системи).

**Умова:** Баланс рахунку повинен бути 0.

#### `get_account_info(buyer_id)`
Повертає повну інформацію про рахунок, включно з історією транзакцій.

#### `list_all_accounts()`
Повертає список всіх рахунків у системі.
### `main.py` - Консольний інтерфейс

**Мета:** Надати зручний спосіб взаємодії користувача з системою.

**Основні функції:**

- `print_menu()` - виводить головне меню
- `create_account_menu()` - інтерфейс створення рахунку
- `deposit_menu()` - інтерфейс поповнення
- `withdraw_menu()` - інтерфейс зняття коштів
- `main()` - головний цикл програми

**Обробка помилок:**
Кожна функція меню містить блоки `try/except` для перехоплення:
- `ValueError` - некоректні дані від користувача
- `TypeError` - невірний тип даних
- `Exception` - несподівані помилки
## 🔍 Приклади використання

### Приклад 1: Створення рахунку та поповнення

```
1. Створити новий рахунок
   ID: ivan_petrenko
   Баланс: 1000
   Категорія: Student

2. Поповнити рахунок
   ID: ivan_petrenko
   Сума: 500
   
   Результат: Баланс = 1500 грн
```

### Приклад 2: Розрахунок знижки

```
7. Калькулятор знижок
   Ціна: 1000 грн
   Категорія: Student
   
   Результат: 
   Знижка: 100 грн (10%)
   Фінальна ціна: 900 грн
```

### Приклад 3: Оптова покупка

```
8. Калькулятор оптової знижки
   Ціна за одиницю: 50 грн
   Кількість: 100 шт
   
   Результат:
   Сума без знижки: 5000 грн
   Знижка: 750 грн (15%)
   Фінальна сума: 4250 грн
```
## 🛡️ Обробка помилок

Система перевіряє та обробляє:

### Валідація даних:
- Порожній ID покупця
- Від'ємний баланс або сума
- Недостатньо коштів для зняття
- Спроба закрити рахунок з ненульовим балансом

### Робота з базою даних:
- Пошкоджений JSON файл
- Відсутність файлу (автоматично створюється)
- Помилки збереження

### Приклад обробки:
```python
try:
    balance = get_balance("user123")
except ValueError as e:
    print(f"Помилка: {e}")  # "Рахунок 'user123' не знайдено"
```
## 📊 Структура JSON бази даних

```json
{
  "ivan_petrenko": {
    "buyer_id": "ivan_petrenko",
    "balance": 1500,
    "category": "Student",
    "transaction_history": [
      {
        "type": "deposit",
        "amount": 1000,
        "balance_after": 1000
      },
      {
        "type": "deposit",
        "amount": 500,
        "balance_after": 1500
      }
    ]
  },
  "maria_koval": {
    "buyer_id": "maria_koval",
    "balance": 5000,
    "category": "VIP",
    "transaction_history": []
  }
}
```
## 🎓 Які теми курсу використовуються

| Тема | Де використовується |
|------|---------------------|
| **Типи даних** | float, int, str, dict, list |
| **Умови** | if/elif/else для перевірок та знижок |
| **Цикли** | while для головного меню, for для списків |
| **Функції** | def для всіх операцій |
| **Колекції** | dict для бази даних, list для історії |
| **Виключення** | try/except для обробки помилок |
| **Робота з файлами** | JSON для збереження даних |
| **Модулі** | import для організації коду |
| **Pathlib** | Path для роботи зі шляхами |
## 🔧 Можливості для розширення

### Ідеї для вдосконалення:
1. Додати пошук рахунків за критеріями
2. Експорт звітів у CSV
3. Додати дату до кожної транзакції
4. Реалізувати систему категорій товарів
5. Додати можливість переказу коштів між рахунками
6. Створити веб-інтерфейс (Flask/Django)
7. Додати статистику (найактивніші клієнти, загальна сума транзакцій)
## ❓ Поширені питання

**Q: Що станеться якщо видалити файл accounts.json?**
A: Програма автоматично створить новий порожній файл при запуску.

**Q: Як відновити дані з резервної копії?**
A: Перейменуйте файл `accounts.json.backup` в `accounts.json`.

**Q: Чи можна мати два рахунки з одним ID?**
A: Ні, ID має бути унікальним. Система видасть помилку.

**Q: Що якщо баланс від'ємний після операції?**
A: Система не дозволить зняти більше, ніж є на рахунку.
## 📝 Завдання для самостійної практики

1. Додайте функцію `transfer(from_id, to_id, amount)` для переказу між рахунками
2. Реалізуйте пошук рахунків за категорією
3. Додайте обмеження на максимальну суму зняття за раз
4. Створіть звіт про всі транзакції за певний період
5. Додайте можливість зміни категорії клієнта
## 👨‍💻 Автор

Міні-проект створено для навчальних цілей в рамках курсу Python.
## 📄 Ліцензія

Навчальний проект - використовуйте вільно для навчання!